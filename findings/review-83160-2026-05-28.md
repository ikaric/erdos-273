# Critic review — NumericalBound83160.lean (2026-05-28)

**Reviewer:** orchestrator (structural review; the dispatched `critic`
subagent failed to produce a report — it attempted to rebuild the file
rather than review its logic). Arithmetic independently cross-checked
in Python (`sympy`/`Fraction`).

**Subject:** `formal/Erdos273/NumericalBound83160.lean`, theorems
`no_strict_admissible_cover_83160` and
`no_strict_admissible_covering_Z_83160`.

## Verdict: PASS (pending build + `#print axioms` confirmation)

The file is the exact structural mirror of the already-verified
`NumericalBound27720.lean`, with `27720 → 83160`. The only new content
is the larger explicit Finset `M83160` and the larger `decide` budgets.

## Checks performed

### 1. Arithmetic (independently verified in Python)
- `83160 = 2³·3³·5·7·11` (128 divisors). ✓
- `M83160` (the explicit 45-element Finset literal) equals exactly the
  set `{m : m ∣ 83160, m+1 prime, m ≥ 4}`. ✓ (Python list-equality.)
- `D(83160) = Σ_{m∈M83160} 1/m = 27241/27720 ≈ 0.9827 < 1`. ✓
- The 11 new elements vs `M27720` are
  `{108, 270, 378, 540, 756, 2376, 2970, 4158, 7560, 8316, 16632}`,
  matching the docstring. ✓

### 2. Statement faithfulness (no hidden weakening)
- `ResidueClass.toSet c = {n : ℤ | (n : ZMod c.modulus) = c.residue}` —
  the genuine congruence class. ✓
- `CoveringSystem.covers : ∀ n : ℤ, ∃ c ∈ classes, n ∈ c.toSet` —
  genuine covering of all of ℤ. ✓
- `IsStrict C := (C.classes.image modulus).card = C.classes.card` —
  genuinely encodes distinct moduli; `isStrict_iff` proves it equivalent
  to `Set.InjOn modulus`, so it is not vacuous. ✓
- `AdmissibleModulus m := ∃ p, p.Prime ∧ 5 ≤ p ∧ m = p − 1` — exactly
  𝓜 = {p−1 : p ≥ 5 prime}. `admissibleModulus_iff` reduces it to
  `5 ≤ m+1 ∧ (m+1).Prime`, which is correct. ✓
- The finite-range theorem hypothesis `∀ i < 83160, ∃ c ∈ S, (i:ℤ) ∈
  c.toSet` together with `covers_int_iff_covers_range_L` (verified in
  `IntegerForm.lean`, used identically by the 27720 ℤ-form) correctly
  lifts to "covers all of ℤ". ✓

### 3. Proof soundness
- `no_strict_admissible_cover_83160` applies the verified abstract
  lemma `no_strict_cover_of_low_density 83160 … M83160 M83160_ge_two
  M83160_dvd D83160_lt_one` exactly as the 27720 proof does. The
  membership hypothesis `hSmem : ∀ c ∈ S, c.modulus ∈ M83160` is
  discharged by `mem_M83160_of_admissible_dvd`. ✓
- `no_strict_admissible_covering_Z_83160` uses
  `covers_int_iff_covers_range_L C.classes 83160 (by norm_num) hdvd`
  to drop from ℤ to `[0,83160)`, then closes with the finite-range
  theorem. Same template as the verified 27720 ℤ-form. ✓
- No `sorry` anywhere in the file. No `native_decide` (only kernel
  `decide`), so `#print axioms` should be clean (the three foundational
  axioms only) — **to be confirmed by the actual build**.

### 4. "Strictly stronger than L=27720" claim
Correct: `27720 ∣ 83160`, so any strict admissible covering with all
moduli dividing 27720 also has all moduli dividing 83160, hence is ruled
out by the 83160 theorem (the 27720 obstruction is *subsumed*).
Additionally the 83160 theorem rules out covers using any of the 11 new
moduli that divide 83160 but not 27720. The manuscript wording "strictly
contains the previous obstruction" is accurate.

## Caveat / dependency
PASS is conditional on the `lake build` completing and
`#print axioms` reporting only `propext`, `Classical.choice`,
`Quot.sound` for all six checked theorems. The `decide` over
`Nat.divisors 83160` (range [1,83161)) is expensive in the kernel
(~25+ min wall-clock observed) but finite; the 27720 analogue compiles
under the same approach at the smaller range. If the build does not
terminate within budget on this machine, the fallback is to restructure
`M83160_eq_filter_divisors` to avoid the range scan (see STATUS note).
