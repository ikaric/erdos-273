# Findings index

Inter-agent notes drop here. Naming conventions:

- `lit-<topic>-<date>.md` — literature survey (librarian)
- `heur-<topic>-<date>.md` — heuristic / numerical (computationalist)
- `deadend-<topic>-<date>.md` — abandoned approach
- `openq-<topic>-<date>.md` — open question surfaced during work
- `review-<topic>-<date>.md` — critic adversarial review
- `decision-<topic>-<date>.md` — orchestrator decision recorded without user input
- `breakthrough-<topic>-<date>.md` — a genuine novel result
- `pivot-<date>.md` — strategic pivot ceremony (`/vector pivot`)

## Entries

- `lit-covering-2026-05-28.md` — librarian — foundational literature survey for Erdős #273 (covering systems with moduli $p-1$, $p\geq 5$). Maps the Hough/BBMST/CFT distortion-method literature, the 22 covering-system Erdős problems on Bloom's index, the Lean 4 `StrictCoveringSystem` type already in `google-deepmind/formal-conjectures` (a free upstream asset), the precise classical Selfridge construction context (6 of 14 moduli in the 120-divisor example already lie in $\mathcal{M}$; the gap is precisely the modulus 2), and a prioritized list of 5 next-step attack vectors plus 5 concrete sub-lemmas the formalist can attempt in one session.
- `heur-sat-2026-05-28.md` — computationalist — SAT encoding `formal/numerics/sat_covering.py` of the bounded-period strict-covering question for Erdős #273. Headline result: UNSAT confirmed for $L \in \{60, 120, 360, 840, 2520, 27720\}$ (by CaDiCaL for the first four, by density-bound counting for the latter two; CDCL times out where density succeeds). For $L \in \{360360, 720720\}$ the density bound no longer applies and both CaDiCaL and kissat fail to settle within reasonable timeouts. Manuscript claim: any Erdős #273 covering must use a modulus that does not divide $27720 = 2^3 \cdot 3^2 \cdot 5 \cdot 7 \cdot 11$. Includes the Lean pseudocode for `strict_covering_density_bound` — the single lemma needed to wire the SAT evidence into the manuscript.
- `heur-density-scan-2026-05-28.md` — computationalist (V5 / Issue #7) — Density-bound sweep beyond $L = 27720$. `formal/numerics/density_scan.py` computes $D(L) = \sum_{m \mid L, m+1 \text{ prime}, m \geq 4} 1/m$ as an exact rational for ~40 candidate $L$'s. Identifies **27+ non-redundant winners** $L > 27720$ with $D(L) < 1$ and at least one admissible modulus not dividing 27720 — each unlocks a verified `no_strict_admissible_cover_L` by mirroring `NumericalBound27720.lean`. Smallest non-redundant winner: $L = 83160 = 27720 \cdot 3$ ($D \approx 0.9827$); largest seen: $L = 3354120$. Also identifies the smallest $L$ with $D(L) \geq 1$: **$L = 55440 = 27720 \cdot 2$** (correcting V1's implicit assumption that 360360 was the first such $L$ — true only on the canonical sequence $\{60, ..., 27720, 360360, 720720\}$ V1 considered). Includes the Mertens-style growth $\sum_{p \leq P} 1/(p-1) \sim \log\log P$ confirming the global density is unbounded, so the structural argument (BBMST distortion) is essential above the empirical $D < 1$ regime.
- `lit-V3-hough-2026-05-28.md` — librarian — V3 (issue #5) deep-dive on Hough 2015 distortion method and its descendants (BBMST 2022 / CFT 2022 / KKL 2022), with focus on specialization to $\mathcal{M} = \{p-1: p\geq 5\text{ prime}\}$. Headline finding: the distortion machinery naturally accepts moduli-set structure as input via the "hyperplane covering of $S_1 \times \cdots \times S_n$" reformulation (BBMST survey Theorem 2.1); CFT 2022's $\leq 118$ bound for squarefree moduli is mechanically that machinery with $|S_j| = p_j$ exactly. The corresponding $\mathcal{M}$-specialization input is the prime-multiplicity distribution $\nu_{p_j}(q-1)$ as $q$ ranges over primes, which has explicit density $1/(p_j-1)p_j^{\gamma-1}$ by Dirichlet/Siegel-Walfisz. Five concrete sub-lemmas listed (§5.1-5.5) ranging from 1-session formalist tasks (density estimate, hyperplane reformulation) to 3-5-session analyst tasks ($\mathcal{M}$-specific moment bound, full density theorem analog). Recommended next step: a Python notebook implementing CFT-style $\delta_j$ optimization with $\mathcal{M}$-density corrections — produces an explicit numerical $\mathcal{M}$-min-modulus bound and is the lowest-effort highest-info-content concrete deliverable for V3.
- `lit-V2-divisibility-2026-05-28.md` — combinatorialist (V2 / Issue #4) — Pins the "every covering contains a divisibility pair" theorem as **Schinzel's 1967 conjecture, proved as BBMST 2022 Theorem 1.2** (arXiv:1811.03547, Inventiones 228, §9; = negative resolution of Erdős #586). Hypothesis-light (only "finite cover of ℤ"; no distinctness, no min-modulus). **Negative leverage assessment for 𝓜:** the theorem is trivially satisfied on 𝓜 (riddled with pairs 4∣12, 6∣18, 10∣30, …), and its only non-existence content (forbidding antichain-coverings) is *strictly subsumed* by the project's verified reciprocal bound — the reciprocal-maximal divisibility-antichain inside $M_{27720}$ is $\{4,6,10,22\}$ with $\sum 1/m = 371/660 \approx 0.562 < 1$, killed by density alone. Recommends **not** building a vector on Theorem 1.2 (cite as SOTA only); productive levers remain the density bound at larger $L$ and the V3 distortion specialization. Documents the V2 divisibility-pair sub-lever as a dead end.
- `heur-cft-Mbound-2026-05-28.md` — computationalist (V3 / Issue #5) — Validation status of the CFT 2022 δ_j optimization specialized to 𝓜. **Squarefree acid test PASSED:** pushing the cut N from 200 to 10⁶ at C₀ = 118, the reproduced sum converges to **TOTAL = 0.99938506**, matching CFT 2022 eq. (12) (0.999385061449) to **8 significant digits**, with the small/mid/tail split each matching eqs. (9)/(10)/(11) independently — the earlier 1.263 at N=200 was purely the loose tail at small N. **𝓜-mode did NOT validate:** the |S_j|^𝓜 = p_j(p_j−1) "density-factor" substitution drops the sum below 1 *even at C₀ = 0* (TOTAL ≈ 0.918), which would absurdly prove 𝓜 cannot cover ℤ at all — the over-reach tell of a mislocated/double-counted density factor (1/(p−1) belongs on the *count of admissible hyperplanes*, not the per-coordinate weight). So no defensible C_𝓜; the "1" reported is an artifact and must stay out of the manuscript. The validated engine (`cft_validate_squarefree.py`) is reusable; the correct next V3 step is the hyperplane-restriction count (librarian §5.2–5.3, an analyst task), not a coordinate-size swap. Also fixed a (harmless, wrong-direction) c₁ bug in `cft_delta_optimization.py` (1/log²p_N, not 1/(2log²p_N)). Scripts: `cft_validate_squarefree.py`, `cft_M_heuristic.py` (+ `.log`s).
- `review-83160-2026-05-28.md` — orchestrator (verify cycle for `NumericalBound83160.lean`) — Structural review of the L=83160 density obstruction (dispatched `critic` failed to report; orchestrator did the structural pass, Python cross-checked the arithmetic). Verdict **PASS** pending build + `#print axioms`: statements faithful (no hidden weakening of `IsStrict`/`AdmissibleModulus`/covering), proof is the exact verified 27720 template with 27720→83160, no `sorry`/`native_decide`, and the "strictly stronger than 27720" claim is correct ($27720 \mid 83160$). Notes the kernel `decide` over `Nat.divisors 83160` (range scan) is the expensive step.
